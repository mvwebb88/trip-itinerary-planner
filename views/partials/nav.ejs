<link rel="stylesheet" href="/styles.css" />

<div class="container">
  <header class="header">
    <div class="brand">
      <div class="brand-badge">🧭</div>
      <div>
        <div>Trip Itinerary Planner</div>
        <div class="badge">Plan • Pack • Go</div>
      </div>
    </div>

    <div class="nav-links">
      <a class="pill" href="/">Home</a>

      <% if (user) { %>
        <a class="pill" href="/trips">Trips</a>
        <span class="badge">Hi, <%= user.name %></span>

        <form action="/auth/sign-out" method="POST" style="display:inline;">
          <button class="btn btn-secondary" type="submit">Sign Out</button>
        </form>
      <% } else { %>
        <a class="pill" href="/auth/sign-in">Sign In</a>
        <a class="btn" href="/auth/sign-up">Sign Up</a>
      <% } %>
    </div>
  </header>
</div>


